# TODO (Audited 2026-05-08)

## Completed
- [x] GUI too large; reduced sizing to avoid excessive scrolling
- [x] Retirement age slider/input typing behavior fixed
- [x] Save state for sliders and checkboxes
- [x] Add ability to save life plans for comparison
- [x] Add career income planning/estimation
- [x] Add life events (house buy/sell, job changes, breaks, etc.)
- [x] Add Retirement tab checkbox to derive current age from DOB
- [x] Add Options tab DOB configuration for age calculations
- [x] Move Add-Ons controls back into Retirement tab
- [x] Add Future Retirement tab based on Careers timeline
- [x] Add student loans and loans
- [x] Remove top blue header bar
- [x] Add Priority field to accounts/pools (determines withdrawal/contribution funding order)
- [x] Add Employer Match % per career (used in projection math)
- [x] Add Import Folder button (webkitdirectory-based CSV/PDF batch import)
- [x] Legacy 4-account removal (types, defaults, storage, engine, app, charts, imports, tests, docs)

## UI Polish & Layout
- [ ] Improve overall UI polish
- [ ] Add vertical splitter/grip to resize left/right panes
- [ ] Make purchases table name column wider (at least ~8 chars)
- [ ] Make filters work on both portfolio graph and stacked savings graph
- [ ] Add 250k option for graph Y-axis legend
- [ ] Stacked savings graph should keep scale when switching from portfolio graph

## Validation & Correctness
- [ ] Validate "money from income" when used as funding source for loans and expenses (including fallback chain)
- [ ] Fix tooltip: inflation compensation text for max withdrawal value
- [ ] Fix career checkboxes
- [ ] Balance at end of period from career timeline does not take into account emergency fund max (hardcoded 15000 limit)
- [ ] Allow multiple accounts to be used as funding sources
- [ ] Add next viable date to long-term purchases and indicate which account is short

## Purchases, Loans & Expenses
- [ ] Expense tracking GUI (sub-tab exists with account selection, tracking view is placeholder)
- [ ] Move loans into purchases and expenses
- [ ] Support displaying large purchase events in expense planner
- [ ] Create dedicated Loan UI and Purchase/Expense UI menus (similar to Expenses tab)
- [ ] Add per-purchase/expense priority to determine funding order

## Importing & Data Ingestion
- [ ] Improve expense import UI
- [ ] Fix statement importing (PDF + CSV)
- [ ] CSV import: column mapping UI (select transaction name, description, amounts; highlight auto-detected mappings)
- [ ] CSV/PDF import: row-level click-to-select, custom row ranges, account name/balance metadata selection
- [ ] CSV/PDF import: inline review/reconciliation GUI

## Retirement & Calculation Logic
- [ ] Income tax on 401k
- [ ] Verify withdrawal taxes are implemented correctly
- [ ] Add 401k-specific retirement withdrawal start date
- [ ] Add ability to change when healthcare gets expensive in retirement
- [ ] Add toggle to enable/disable inflation adjustment for maximum yearly withdrawal
- [ ] Minimum retirement withdrawal can come from any account with remaining balance (not blocked by 4% rule path)
- [ ] Modify retirement failure conditions (enforce minimum yearly withdrawal, add slider/control, ensure math uses it)
- [ ] Add theoretical early retirement year/age in graph summary message
- [ ] Net worth history logging

## Accounts, Pools & Configuration
- [ ] Replace hardcoded 4-account model with fully generic accounts + per-account rule designation (401k, Roth, HSA, etc.)
- [ ] Configure savings account colors (global per-account, not just pool-level)
- [ ] Add account/credit card due date option
- [ ] Add credit card pool
- [ ] Emergency fund max: add option per account (0 = no cap) + fallback destination for overflow
- [ ] Add lock for time period / specific account (lock net worth updating while allowing transactions)
- [ ] Make flag colors based on account colors; stacked savings graph should also use per-account colors

## Tabs, Structure & Sessions
- [ ] Net worth tab should use table/graph from Finances Prediction
- [ ] Add menu to save session, start new session, load saved session (local only)
- [ ] Add compare tab for graph overlays
- [ ] Add spouse/second plan overlay support
- [ ] Incorporate budgeting
- [ ] Timeline manager: drag, reorder, delete career items
- [ ] Make Options page display same graph/table/message as Finances Prediction
- [ ] If 2+ careers occur at same time, stack them vertically

## Export & Interop
- [ ] Create Excel export
- [ ] Add Docker packaging
- [ ] Replace local placeholder historical series with web-backed historical market data
- [ ] Calendar integration (including expense events)
- [ ] Give user ability to configure date format in Options
- [ ] Export finances as generic formulas for Excel/manual calculations
- [ ] Make entire GUI color configurable

## Uncommitted Changes (working tree)
- [ ] Review uncommitted diff: taxInfo replaces takeHomePay on CareerEntry (types, defaults, storage, projection, tests, CareerPlanEditor)
- [ ] Review uncommitted diff: fallbackDetails + firstFallbackYearMonth on incomeFundedItemStatuses for detailed tooltip display
- [ ] Review uncommitted diff: purchase processing order changed (loans processed before purchases)
- [ ] Review uncommitted diff: removed Difference column from long-term purchases table
- [ ] Review uncommitted diff: tsconfig.app.json moduleResolution changed from Node to Bundler

loans table should only check if income is valid for events that have 

Create a Debugger. When debug mode is enabled, any value can be right clicked and inspected. The inspection displays  table showing the sequence of math and order of operations interest, teaxes etc that were used in the calculation of the value. This can also be used to see what a value is used for.

Add Tax bracket configuration.